package component;

public enum Operation {
    CREATE, //create archive
    ADD, //add files
    REMOVE, //remove files
    EXTRACT, //extract all data
    CONTENT, //show files o=in the archive
    EXIT
}
